version: "3"
services:
  app:
    build: .
    environment:
      DATABASE_URL: postgres://user:pass@postgres:5432/db
      NODE_ENV: development
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - .:/csv
    networks:
      - sql_csv

networks:
  sql_csv:
    external: true
